EC 04/05/26: Unified sigma controls: the separate initial-width and collapsed-width parameters were replaced by a single "Measurement σ" slider (range 0.7–2.0, default 0.75) that sets both the initial wavepacket sigma and the post-collapse sigma, so the packet re-localizes at the same width after each measurement. The slider label was changed from the word "sigma" to the Greek symbol σ. Heatmap grid lines were realigned to 1-unit physical spacing on both axes, with major lines (multiples of 5) drawn brighter. The Measure button was restyled blue and moved to its own section above the View toggle for prominence. A "Lecture display" checkbox was added under Visuals that hides the Statistics and Recent Collapses panels to declutter the screen during live lectures. Full physics audit confirmed correctness of the spreading formula, 2D normalization, Born-rule sampling, post-collapse re-spreading, and erf approximation.

EC 03/30/26: The simulation was audited against Lecture 15 ("Measurement") and Lecture 6 ("The Gaussian Wavepacket") for physics correctness, visualization integrity, and lecture alignment. The 3D soft-cap and heatmap peak-normalization were replaced with honest fixed-scale displays (linear height with clamp, fixed color scale with legend), the "Peak Height" stat was corrected to show the actual |ψ|² value, z-axis ticks now show physical units, the time label was fixed for natural units (ℏ = m = 1), and the about tooltip was updated to reference the Born rule and collapse postulate. The 3D/Heatmap toggle was replaced with a view-mode button per user request, the redundant "State" row and vague "Quantum Statistics" label were removed, and the heatmap renderer was optimized to a fixed 256×256 physics grid with a pre-computed color LUT to eliminate per-frame timeouts.
