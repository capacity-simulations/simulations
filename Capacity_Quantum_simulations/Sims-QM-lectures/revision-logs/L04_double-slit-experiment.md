EC 03/30/26: = The classical view was changed so a broad spray of particles hits the barrier, only particles that pass through the two slit openings continue, and the detected orange pattern is the smooth sum `P_1 + P_2` of two one-slit bullet lobes whose separation changes with `d`. The explanatory text and controls were then aligned with that interpretation by relabeling the classical pattern, clarifying the on-screen descriptions, and disabling the wavelength control in classical mode because the bullets model does not depend on `\lambda`.
BS 04/06: = Added single slit blocking fnctionality for both the classical and quantum modes. Now one of the slits can be blocked to see the single slit diffraction pattern in the quantum mode and the single classical gaussian spread in the classical mode. Incraesed the wavenegth range on the lower side to ensure the required a/lambda ratio to observe the single slit diffraction pattern. Fixed the detector pattern bug (for certain param combinations, the central maxima peak used to go below the secondary maximnas). Fixed the classical pattern on the screen to follow the spreading synced with the detector plot pattern.

 BS 04/06: = Added the lecture display checkboc to the controls.
EC 04/09: = Default wavelength changed to 525 nm. Slit toggle (S1/S2 open/close) no longer resets any slider values (d, a, λ, L, rate) — all controls stay at the user's selected values when toggling slits. Fixed histogram normalization: replaced peak-normalization (`hits[i]/max(hits)`) with area-matched normalization (`hits[i] * theorySum / totalHits`) to eliminate systematic downward bias at early times. Changed theory curve sub-sampling from max-in-bin to mean-in-bin so the theory represents the same bin-average probability that the Monte Carlo measures.