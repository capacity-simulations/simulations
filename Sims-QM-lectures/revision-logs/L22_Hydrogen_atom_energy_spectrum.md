EC 04/05/26: The hydrogen emission-spectrum simulation was updated to make the lecture version cleaner and easier to present by moving the animation-speed control below the spectral-series buttons, renaming that section to `Visuals`, and adding a `Lecture display` checkbox that hides the mode toggle, the explanatory transition note, the last-transition panel, and the bottom status readout when a simpler layout is preferred. The title/header was extended across the full top of the simulation and given a header-level `Reset` button, the sidebar `Controls` heading was removed, and the spectrum-strip title styling was matched to the `Energy Levels (eV)` title for a more consistent visual hierarchy. The series playback timing was also corrected so each Lyman/Balmer/Paschen transition waits for the previous photon to reach the strip before the next one begins, with a small pause between transitions, making the sequence easier to follow during lecture.
