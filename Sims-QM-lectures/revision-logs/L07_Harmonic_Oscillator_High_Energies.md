EC 04/02/26: The high-energy harmonic oscillator simulation (`L07_Harmonic_Oscillator_High_Energies.html`) was aligned with Lecture 7 notes and the companion `L07_harmonic-oscillator.html` layout: the potential, wavefunction, and probability panels are stacked vertically; visible notation uses \(x\) and \(\psi_n\) (including titles and axes) instead of a dimensionless \(\xi\) label; the sidebar was reduced so the main readout is \(\Delta E / E_n\) (with controls for \(n\) and animation speed). The misleading \(\pm|\psi|\) envelope was removed. The classical dwell-time curve \(P_{\mathrm{classical}}(x)\) stays only on the probability-density panel, with a stronger orange stroke, translucent fill under the curve, and the orange line drawn after the green \(|\psi_n|^2\) trace so it reads on top. In-plot legends sit in the top-right (with readable subscripts on labels where HTML allows, and canvas-safe text on axes); maximize/restore controls match the small icon style from `L02_Two_Gaussian_superposition.html`. Default animation speed is \(0.1\) with the numeric display suffixed by `x`. These changes were made for lecture fidelity, clearer classical–quantum comparison at high \(n\), and consistency with the rest of the lecture sim set.
