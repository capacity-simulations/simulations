EC 03/30/26: The simulation was updated to add a 3d visualization for the circle. The notes were more closely matched by removing the `L_z` sidebar/display, adding the lecture-facing energy, wavefunction, and probability-density equations, and correcting the energy ladder so it uses true `E_n \propto n^2` spacing with a dynamic top-of-axis scale. The ring view was then cleaned up to improve interpretation and usability by fixing the default orientation and drag behavior, smoothing high-`n` plotting, clarifying the axis/radius annotations, and simplifying the on-screen guidance text.
EC 04/05/26: The lecture version was cleaned up to reduce visual clutter during presentation. The `Animation` heading was renamed to `Visuals`, a default-on `Lecture display` checkbox was added to hide the doubly-degenerate note plus the `Current State` and `Lecture Equations` sections when a cleaner layout is preferred, and the MathJax styling was changed so all rendered LaTeX inherits the surrounding text color for a more consistent appearance.
EC 04/09/26: The plot rendering was sharpened for high-DPI displays by sizing both canvases at device-pixel resolution while preserving the existing drawing coordinates and physics. The flat plot notation was also adjusted to use `x` on the displayed axes instead of `\phi`, and the non-legend text across the energy plot, flat plot, and ring axes was enlarged for better lecture readability.
