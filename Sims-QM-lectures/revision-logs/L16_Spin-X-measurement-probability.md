EC 04/04/26: The simulation was audited against Lecture 16 ("The Qubit" / spin time evolution in a magnetic field) for physics correctness, visualization integrity, and lecture alignment. The display was tightened to follow the notes more closely: the x-basis labels were restored to `|\rightarrow\rangle` and `|\leftarrow\rangle`, the state description and `P_+` equation were simplified to match the lecture convention with real `\alpha,\beta` and `\beta=\sqrt{1-\alpha^2}`, and extra derived readouts that were not part of the lecture presentation were removed. A linked `\beta` slider was added so either coefficient can be adjusted while preserving `\alpha^2+\beta^2=1`, maximize/restore controls were added for the Bloch sphere and probability plot, and a `Lecture display` mode was introduced to hide non-lecture controls (`\mu B`, speed), output readouts, the `P_+` equation block, and the moving plot marker while defaulting to the maximized probability view. The Bloch vector’s moving `n` label was removed for clarity, plot header/title spacing was cleaned up, the tiny legend was removed, and the probability plot margins and live-value placement were adjusted so the y-axis label, tick labels, and cursor values render with safer spacing and less crowding.

EC 04/09/26: The Bloch-sphere axis-label placement was refined so labels stay inside the canvas when the plot is tight against the frame. In particular, the `z` label in the top plot was being clipped near the boundary, so the axis-label draw position now clamps to a small safe inset from the canvas edges instead of allowing labels to render partially offscreen.
