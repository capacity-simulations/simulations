# Engine Changelog

Every PR that touches a module MUST append an entry under the pending version:
`- <module>: <breaking|additive|fix> — <one-line physics impact> (card updated: yes/no)`

## 1.0.0-dev (unreleased)
- core.scale: additive — initial harvest: makeScale linear map factory (collapses ~12 inline copies), clamp, lerp, fmt with -0/U+2212 handling from CM Collisions.html (card updated: yes)
- tooling: additive — registry.json, manifest schema, build.py (manifest → inlined window.Engine IIFE with version/hash stamp), lint_cards.py (card updated: n/a)
- canvas.fit: additive — initial harvest: DPR-aware fit() merging CM L6 (setTransform reset, fixes cumulative-DPR blur) with SR L09-s1 (max(1,…) zero-size guard) (card updated: yes)
- canvas.scale: additive — initial harvest: makeView world↔screen mapping (SR L09-s1 aspect-locked centered view + CM L36-Scattering inverse for dragging), y-flip owned by the view; namespace Engine.view (card updated: yes)
- canvas.ticks: additive — initial harvest: niceTicks 1/2/5×10ⁿ placement (QM Single_Gaussian_plot genTicks incl. dedupe + degenerate guard) and tickLabel with per-axis-uniform decimals and U+2212 minus (card updated: yes)
- canvas.axes: additive — initial harvest: drawAxes grid/tick/label/frame (CM L4-Projectile drawPlot generalized to any view) and makeStripChart clipped rolling multi-series chart with tip dots (CM L17 draw() infra) (card updated: yes)
- canvas.arrow: additive — initial harvest: drawArrow endpoint signature replacing 13 variants; CM L4-Forces head geometry + off-canvas truncation glyph via opts.clip (card updated: yes)
- canvas.label: additive — initial harvest: labelAt name collision retired — textBox (CM L4-Projectile, canvas-bounds clamping) and callout (CM L4-Forces, radial leader-tip placement) (card updated: yes)
- canvas.primitives: additive — initial harvest: roundRect (CM L35-Orbits, radius clamped), dashLine (factored setLineDash idiom), cssVar with fallback (CM L36-Scattering), diamond (CM L6) (card updated: yes)
- tooling: fix — build.py resolves cross-module imports by registry-relative path instead of basename (core/scale.js vs canvas/scale.js collision) (card updated: n/a)
- shell: additive — canonical Shell runtime harvested from SR L09-s1 L618–816 with all 57-copy drift features folded per shell/HARVEST-NOTES.md (stepUnready L37, playLocked/#play-hint Galperins, setPlayEnabled/gating L36-Energy, finishInquiry/onComplete L9, data-manual-gate+inqNext FBD, scroll-to-top L10-s2, onExploreMode+aliases L00-s1, fastforward landing L19-s2, speed markup boot-read L1-Solar, dot-done stepReady L35, formal-open L07-s1); cfg flags with decided defaults (dtClamp 0.05, resetBeforeFirstStep reset-first, resumeOnReset tri-state); opt-in fixed-dt accumulator frame mode from QM QTGW L2230 (card updated: yes)
- shell (css): additive — shell/shell.css harvested verbatim from SR L09-s1 (top of <style> to the SIM CSS divider, header contract comment preserved) plus the #play-hint contract CSS from CM Galperins_Billiard (wired by setPlayLocked) (card updated: n/a)
- shell.hide-text: additive — optional Hide-Text runtime (window.HT predicate + #ht-toggle wiring + lecture-mode boot default) from CM L6-The harmonic oscillator L446–470 (card updated: yes)
- controls.bind: additive — bindSlider/bindToggle/bindSelect merging QM Plane_Wave wireSlider (clamp + NaN guard) with SR L07-s1 bind (state-key + fmt); bounds read from the element's own min/max; set()/refresh() returned per binding (card updated: yes)
- tooling: additive — build.py supports a registry "alias" key emitting `window.<Alias> = Engine.<ns>;` after a module IIFE (shell → window.Shell keeps the frozen 57-sim Shell API) (card updated: n/a)
- core.integrate: additive — initial harvest: stepRK4 (CM L16 generalized to any array state), stepVerlet (CM L37, in-place arrays, a0 buffer-copy fix), stepSemiImplicit (CM L4-Forces rate-first idiom), stepRotate (CM L30-Sphere exact norm-preserving planar rotation), stepAdaptive (CM L36-Scattering dt heuristic generalized to step-doubling error control), makeEventStepper (Galperins event engine with caller-supplied detect/resolve, Zeno cap); validated against l9/l17/l36/l7 goldens + the Galperin 31-collision signature (card updated: yes)
- core.roots: additive — initial harvest: bisect (CM L7, tol/maxIter + bracket validation added), newton (CM L36-Energy generic loop only, Kepler equation stays in domain), bracketScan (QM Bound_states branch scan generalized to uniform intervals, non-finite endpoints skipped); validated against l7 golden equilibria/turning points (card updated: yes)
- core.complex: additive — initial harvest: cMul/cDiv/cExp/cSqrt (QM Time_evolution_Gaussian_wavepacket L947–974 verbatim) + cAdd/cSub/cScale/cAbs2; in-place split-array ops arrMulPhase/arrAbs2/arrNormSq factoring the e^{i phi} rotation idiom from QTGW/ANHARMONIC; validated by algebraic identities + normalized-Gaussian norm (card updated: yes)
- core.fft: additive — initial harvest: in-place radix-2 fft (QM Quantum_Tunneling_Gaussian_Wave L1187–1238) with the power-of-2 RangeError assert MISSING in both source copies (silent data corruption fixed), + freqGrid FFT-ordered angular-frequency helper; validated impulse/DC/tone, Parseval, 1024-pt Gaussian vs analytic transform (card updated: yes)
- core.special: additive — initial harvest: hermiteNorm stable orthonormal recurrence (QM Harmonic_Oscillator_High_Energies — NOT the overflowing naive Harmonic-oscillator version), assocLegendre with condonShortley=true default matching scipy lpmv and both source recurrences (Explorer negative-m branch kept; Hydrogen's |m| substitution documented), assocLaguerre (Hydrogen_atom_wavefunctions), erf A&S 7.1.26 (2d_wavefunction_collapse, ~1.5e-7); validated vs checked-in scipy 1.17.1 table test/golden/special-values.json (card updated: yes)
- core.sample: additive — initial harvest: makeGaussian cached-spare Box–Muller (QM 2d_wavefunction_collapse) and sampleFromPDF generalized rejection sampler (QM infinite-potential-well) — BEHAVIOR CHANGE: attempt-cap exhaustion and envelope violations throw RangeError instead of the source's biased midpoint fallback; all randomness via injectable rng; validated by seeded moments + KS vs sin^2 CDF (card updated: yes)
- tooling: additive — test/capture/gen-special-values.py generates the scipy reference table for core.special (card updated: n/a)
- verify.audit: additive — defineAudit formalizes the window.__audit convention (ad-hoc in 35 sims) as {version: 1, manifest, probes, invariants, state, setParam, run()}; manifest parsed from #engine-manifest, run() aggregates {ok, value, expected, tol} invariant records, default setParam drives controls via value + 'input' dispatch (the controls.bind code path); node-testable via an injectable target (card updated: yes)
- verify.guard: additive — numerics guard genericized from QM Classical-vs-schrodinger-ANHARMONIC L695–847 + L1044–1110: makeGuard with monitor factories normDrift (1e-4/1e-3), invariantDrift (1e-3/1e-2, caller-labeled), highFreqOccupancy (0.9·Nyquist cutoff, 1e-6/1e-5), finite (NaN/Inf → halt); auto-halt with plain-English reasons and check() frozen until reset(); recommendSubsteps ports the phase-per-substep bound (ceil(rate·dt/0.08), clamp [5,80]); registry deps [] on purpose — audit integration is registerWith(audit) plus runtime __audit detection, NOT an import, so guard loads without audit and verify modules stay mutually independent; fidelity-tested against the source's warn/halt/substep decisions (card updated: yes)
- domain.relativity: additive — initial harvest: gamma (one name retiring 8 spellings, RangeError guard at |beta| >= 1), boost2/unboost2 (SR L18-s1 toFrame/fromFrame = the -new trio boost), boost4/minkowskiNormSq (SR L13-s1 boost/mdot extended to [ct,x,y,z]), interval with caller/input-relative lightlikeEps (fix #10 — no baked 0.035), rapidity/betaFromRapidity, addVelocities (the only sanctioned velocity composition), properTimeAlong (SR L09-s1 seg/pathInfo; spacelike segment → NaN instead of the source's zero+flag); validated against sr-l09/l13/l14/l17 __audit goldens at 1e-12 (card updated: yes)
- domain.mechanics: additive — initial harvest: solveKepler elliptic + hyperbolic branches (CM L36-Energy advance L867–910; fixed Newton sweeps become core.roots newton tol 1e-15 with bisect fallback; e = 1 throws — Barker stays sim-side), orbitFromPeriapsis (L37 initAtPerihelion/sampleOrbit + T = 2π√(a³/mu)), pendulumPeriodRatio (L9 agmRatio AGM verbatim — the __audit formula deferred at M3), dragForceLinear/dragForceQuadratic/terminalVelocity (L16 dragFlight/quadFlight closed forms), restitutionCollision1D/explosionOutcome (Collisions computePhysics; momentum conserved by construction, ΔKE = −½μ(1−e²)u²), effectivePotential/classifyOrbitByEnergy (L36 Veff/regimeName; sub-minimum E throws instead of the source's silent clamp); validated vs l9-pendulum, l16-friction, collisions goldens (card updated: yes)
- domain.quantum: additive — initial harvest: makeGrid/gaussianPacket (QTGW init + numeric normalize), makeSplitOperator Strang propagator with cached-per-dt phase arrays and absorbing boundary + absorbed-probability accounting (QTGW computePhaseFactors/timeStep/applyAbsorbingBoundary L1259–1444, in place via core.complex arrMulPhase + core.fft), prob/normSq/expectation('x'|'p'), findBoundStates finite-well branch scan (Bound_states L1214–1297 via core.roots bracketScan + bisect), transferMatrix backward A/B propagation incl. the E≈V degenerate-linear branch with input-relative eps (Plane_Wave L1417–1625; source's absolute 1e-6 eV threshold generalized), transmissionRect closed form rewritten in FULL width (Transmission_probability_plot computeT L676–700), radialRnl (Hydrogen_atom L279–302 via core.special assocLaguerre; source's numeric-quadrature norm replaced by the closed-form factorial-ratio norm), sampleDetection rejection over discrete |ψ|² via core.sample; validated vs analytic dispersion/finite-well/closed-form-T/orthonormality/KS references in-test (card updated: yes)
- tooling: additive — make_prompt.py generation-context assembler (the leakage firewall): human-selected modules validated against the registry (unknown name = hard error, no include-all flag), verify.audit force-included, deps resolved via build.py's resolver (import, not copy — prompt order = build order), fixed assembly preamble → verbatim manifest → module cards → domain card(s) → cross-domain bridge when >1 domain family → shell/verify contracts → author spec; chars/4 token estimate with 8k hard ceiling naming the biggest cards (card updated: n/a)
- tooling: additive — contract cards cards/_preamble.md (generation hard rules), _shell-contract.md (condensed Shell.init/cfg/DOM-ID/frame-mode contract), _verify-contract.md (defineAudit surface + when verify.guard is required), _cross-domain.md (multi-domain bridging rules: constraints stay absolute per panel, coupling only via shared params/read-only overlays); lint_cards.py now checks their existence + budgets (400/800/400/350) and that domain cards carry >= 3 NEGATIVE CONSTRAINTS bullets; reference example in briefs/ (example-tunneling brief + assembled prompt) (card updated: n/a)
- domain.relativity-diagram: additive — initial harvest of the spacetime-diagram kit per domains/relativity/DIAGRAM-HARVEST.md: drawSGrid/drawSAxes (-new L05 four-quadrant), drawPrimeGrid/drawPrimeAxes + PURE primeTickPositions (-new L18 γ-spaced grid, hyperbola ticks at (γk, γβk)), drawLightCone (-new L18 wedge, -shell rect/clip fix), clipSegment (-shell Liang–Barsky), normalizeWorldline/drawWorldline (-shell time-forward fix #4, superluminal dash + ⚠ label), drawEventMarker (-shell radial labels), classifyPair (thresholds relative to snap grid, fix #10), makeDraggableEvents (-shell nearest-hit fix #8, 4px click-vs-drag, snap-on-drop, long-press/right-click delete, injectable element/timers); all primitives clipped to view.world (fixes #6/#7); CONFIGS stays sim-side (fix #3) (card updated: yes)
- tooling: additive — verify.mjs per-sim checker (M7a): static pass (node --check per script block; manifest validation replicated in JS from build.py validate_manifest — no python3 dependency; build-stamp sha256 recomputed on build.py's exact pre-stamp hashing boundary; Engine.<ns>/alias reference check vs transitively-resolved manifest modules — hand-rolled-API detector) + dynamic pass on the harvest-goldens puppeteer-core/headless-Chrome recipe (console/pageerror = fail, __audit v1 within 15 s, __audit.run(), min/default/max param sweep via setParam + 10-rAF settles, default-state screenshot to engine/_review/shots/); verdict JSON via --json, nonzero exit on failure; dynamic skipped when static fails so broken-sim triage never needs Chrome (card updated: n/a)
- tooling: additive — check_all.py folder fan-out: classifies .html as legacy (no manifest) vs engine (prints engine version from the build stamp), runs verify.mjs per engine sim (--list-only to classify without verifying, --engine-only to hide legacy), summary table + nonzero exit on any engine failure (card updated: n/a)
- shell (css): fix — @font-face src switched from dangling relative .ttf url() (ERR_FILE_NOT_FOUND console errors in unbuilt sims — verify.mjs fails on any console error) to non-fetching local() lookups; system-font token fallback unchanged, vendor:fonts build step still swaps in embedded fonts (card updated: n/a)
- core.sample: additive — makeRng(seed): seeded LCG (Numerical Recipes u32 constants) returning uniforms in [0, 1), the sanctioned reproducible rng for sim sampling and __audit sweeps (card updated: yes)
- cards: fix — M7b pilot ambiguity sweep: quantum card documents makeSplitOperator's V as a grid.n sample array, step's dt·substeps total-time semantics, absorber sample-units/defaults, and gaussianPacket's sigma convention (Δx = σ, Δk = 1/(2σ)); guard card gains the create-after-state constraint and split-operator maxRate guidance; shell contract gains the markup skeleton, shell.css copy note, and wall-clock dt sentence; verify contract documents that initialRun is not t = 0 (card updated: yes)
- tooling: additive — build.py now injects CSS: shell/shell.css (whenever the shell module is manifested) and vendor assets declared inline:"style" go into an auto-created <style id="engine-css"> block, replaced idempotently each build; the build hash still covers the JS block only. Closes the gap found independently by the M7b pilot and M7c backport ("copy shell.css manually"). cards/_shell-contract.md updated accordingly (card updated: yes)
- pilots: additive — M7b pipeline pilot pilots/qm-tunneling-pilot.html: first sim authored purely from a make_prompt.py-assembled prompt; VERIFY PASS in 3 iterations; quantum-card firewall held (zero classical-trajectory code/vocabulary); produced the 10-item card-improvement list applied above (card updated: n/a)
- M7c: first opportunistic backport — pilots/harmonic-oscillator-backport.html rebuilds Sim_lab_sims/QM_sims/Harmonic-oscillator.html on the engine (core.special hermiteNorm, canvas fit/view/axes, controls.bind, Shell, verify.audit). Kills the original's latent DPR bug by construction; stable Hermite recurrence extends the n slider 0–10 → 0–30; psi_n agreement with the original ≤ 3.5e-15 at n ∈ {0,3,7,10,30}; verify.mjs PASS. Original file untouched (card updated: n/a)
- domain.em-fields: additive — M8, FIRST FRESH (non-harvest) domain module, written directly against core APIs: makeCharges/fieldAt/potentialAt (Coulomb superposition, k = 1, q in units of e, Plummer softening), traceFieldLine (RK4 arc-length streamlines of the normalized field via core.integrate stepRK4, stop at charge/bounds/stagnation), fieldLineSeeds (per-charge circles, count ∝ |q|, dir by sign), equipotentialAt (marching squares + Newton refinement of vertices onto the exact level; generic core — future core/contour candidate), lorentzForce, makeChargedParticle + stepParticle (RK4 on [x,y,vx,vy]; pure-B motion routed through the exact norm-preserving stepRotate chord update — cyclotron radius/period exact to float rounding). Physics tests: dipole 1/r³, superposition, radial line departure + termination on − charges, contour⊥field < 1e-3, cyclotron radius+closure 1e-10 over 100 periods, E×B drift 1e-6 (card updated: yes)
- pilots: additive — M8 pilot pilots/em-fieldlines-pilot.html: point-charge field-line explorer (single/dipole/two-like presets, q1/q2/separation sliders from the params schema, equipotential toggle); defineAudit probes fieldAtPoint/potentialAtPoint + invariants dipoleFalloff (measured falloff exponent vs net-charge expectation) and lineCount (traced lines = seeded expectation); VERIFY PASS (card updated: n/a)
