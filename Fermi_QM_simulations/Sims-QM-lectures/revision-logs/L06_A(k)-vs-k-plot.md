EC 04/01/26: The A(k) plot was streamlined for lecture use by dropping nanometer units, the on-plot legend, the annotation panel and canvas labels, and the redundant k₀ and σ line under the live formula, so attention stays on the Gaussian and parameters. The draggable readout was renamed to a “marker,” drawn without a vertical rule, with σ restricted to ≥ 0.6, the vertical scale capped at A(k) = 1.0, and the pulsing peak glow and particle decoration removed to reduce visual noise and unnecessary motion.

EC 04/02/26: The plotted and KaTeX-displayed A(k) were aligned with the lecture notes (Gaussian in exp with (k−k₀)²/(2σ²) plus the k₀²/(2σ²) term), the y-axis was made dynamic with readable tick formatting for large amplitudes, and the about text was adjusted so it no longer described the old normalized |A(k)|² probability picture.

EC 04/09/26: The ±σ and ±2σ shading was removed from the lecture-facing controls and disabled by default because the lecturer defines σ differently, so those highlighted regions would be misleading in this version.
